
#esses comandos abaixo devem ser rodados no terminal e não dentro desse script
sudo taskset -a -c 0-7 ./bin/scheduler > /dev/null 2> stderror_schedule; sudo tar -cf 1.tar std* *.energy; sleep 5; sudo rm std* *.energy; sudo taskset -a -c 0-7 ./bin/scheduler > /dev/null 2> stderror_schedule; sudo tar -cf 2.tar std* *.energy; sleep 5; sudo rm std* *.energy; sudo taskset -a -c 0-7 ./bin/scheduler > /dev/null 2> stderror_schedule; sudo tar -cf 3.tar std* *.energy; sudo rm std* *.energy; sleep 5; 
